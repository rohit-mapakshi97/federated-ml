model: LGR
num_rounds: 10
num_clients: 100
num_classes: 10
num_clients_per_round_fit: 100
num_clients_per_round_eval: 10
max_attack_ratio: 0.25
attack_round: FULL
attack_type: LF
config_fit:
  local_epochs: 1
  penalty: l2
  warm_start: true
